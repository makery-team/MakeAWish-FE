# Antigravity 2.0 -> IDE Handover Context

## 완료된 작업 (UI/UX 100% 싱크 완료)
이전 Antigravity 2.0 세션에서 사장님 웹 앱(Owner App)과 소비자 모바일 앱(Consumer App)의 UI/UX 디자인을 완벽하게 동기화했습니다.
- **폰트:** `index.tsx`, `header.tsx` 등 주요 컴포넌트의 제목과 텍스트에서 굵은 `fontWeight` 스타일을 제거하여 '고운 돋움' 폰트 특유의 얇고 단아한 감성을 살렸습니다.
- **로고 이모지:** 상단 로고 이미지를 🍰 텍스트 이모지로 교체했습니다.
- **스플래시/온보딩 화면:** `login.tsx` 배경에 노란색과 연보라색의 은은한 글로우(Glow) 효과를 추가하여 웹 앱과 동일하게 구현했습니다.
- **테마 배경색 통일:** 
  - 앱 전역(`index.tsx`, `explore.tsx`, `chat.tsx`, `mypage.tsx`, `shop/[id].tsx`)의 배경색을 크림(아이보리)색(`theme.colors.background`)으로 통일했습니다.
  - 상/하단 바(`header.tsx`, `_layout.tsx`)의 배경을 투명하게 처리하여 크림색 배경이 자연스럽게 이어지도록 했습니다.
  - 채팅창 바텀 시트는 눈에 띄도록 하얀색(`theme.colors.surface`)으로 변경했습니다.

## 🚀 다음 진행할 백엔드 API 연동 과제
아래 작업들 중 하나를 선택해서 에이전트에게 지시해 주세요.

1. **`401 에러(토큰 만료)` 버그 픽스 (가장 시급)**
   - 찜/리뷰 클릭 시 발생하는 토큰 만료 에러를 해결하기 위한 자동 갱신 처리
2. **홈 화면 케이크 피드 리얼 데이터 연동**
   - 백엔드에서 받아온 실제 케이크 피드(사진 목록)를 앱 홈 화면에 렌더링
3. **'주문 생성(Order)' API 연동**
   - 대망의 결제/주문 생성 API를 연동하고 사장님 앱에서 주문 확인해보기
